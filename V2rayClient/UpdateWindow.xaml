<Window x:Class="V2rayClient.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="软件更新" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="Resources/app.ico">
    
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Image Source="Resources/app.ico" Width="32" Height="32" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock x:Name="TitleTextBlock" 
                          Text="发现新版本" 
                          FontSize="18" 
                          FontWeight="Bold"/>
                <TextBlock x:Name="VersionTextBlock" 
                          Text="v1.0.1 → v1.0.2" 
                          FontSize="12" 
                          Foreground="#666666"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- 更新说明 -->
        <GroupBox Grid.Row="1" Header="更新说明" Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesTextBlock" 
                          Text="正在加载更新说明..." 
                          TextWrapping="Wrap"
                          Margin="8"
                          FontSize="12"
                          LineHeight="18"/>
            </ScrollViewer>
        </GroupBox>

        <!-- 进度信息 -->
        <StackPanel Grid.Row="2" x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,0,0,20">
            <TextBlock x:Name="ProgressTextBlock" 
                      Text="准备下载..." 
                      Margin="0,0,0,8"/>
            <ProgressBar x:Name="ProgressBar" 
                        Height="6" 
                        Minimum="0" 
                        Maximum="100" 
                        Value="0"/>
        </StackPanel>

        <!-- 文件信息 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="文件大小: " FontSize="12" Foreground="#666666"/>
            <TextBlock x:Name="FileSizeTextBlock" Text="计算中..." FontSize="12" Foreground="#666666"/>
            <TextBlock Text=" | 发布时间: " FontSize="12" Foreground="#666666" Margin="16,0,0,0"/>
            <TextBlock x:Name="PublishDateTextBlock" Text="--" FontSize="12" Foreground="#666666"/>
        </StackPanel>

        <!-- 按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SkipButton" 
                   Content="跳过此版本" 
                   Style="{StaticResource SecondaryButtonStyle}"
                   Click="SkipButton_Click"/>
            <Button x:Name="RemindLaterButton" 
                   Content="稍后提醒" 
                   Style="{StaticResource SecondaryButtonStyle}"
                   Click="RemindLaterButton_Click"/>
            <Button x:Name="UpdateButton" 
                   Content="立即更新" 
                   Style="{StaticResource ModernButtonStyle}"
                   Click="UpdateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>